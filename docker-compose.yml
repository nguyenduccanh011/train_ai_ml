
services:
  postgres:
    image: postgres:16-alpine
    container_name: stock-ml-postgres
    environment:
      POSTGRES_DB: stockml
      POSTGRES_USER: stockml
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-stockml_dev}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      # Host 5433 → container 5432: avoids clashing with a native Postgres that
      # may already own host:5432. Containers still reach it at postgres:5432.
      - "5433:5432"
    networks:
      - stock-ml-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stockml -d stockml"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  api:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.api
    container_name: stock-ml-api
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
      - DEBUG=False
      - API_PORT=8000
      - DATABASE_URL=postgresql+asyncpg://stockml:${POSTGRES_PASSWORD:-stockml_dev}@postgres:5432/stockml
      - DB_ENABLED=true
      - STOCK_DATA_DIR=/app/market_data/market.duckdb
      # Overlay sandbox: score against the SAME pinned store the board backfill used, so
      # live sandbox numbers match the leaderboard's cagr_overlay column exactly.
      - OVERLAY_MARKET_DB=/app/overlay/market_data/market_golden_pin_20260729.duckdb
      - OVERLAY_OHLCV_DB=/app/overlay/data/ohlcv_golden_pin_20260729.db
      - OVERLAY_DATE_HI=2026-07-08
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./logs:/app/logs
      - ./portable_data:/portable_data
      - ./market_data:/app/market_data:ro
      - "C:/Users/DUC CANH PC/Desktop/stock-serving/market_data:/app/overlay/market_data:ro"
      - "C:/Users/DUC CANH PC/Desktop/stock-serving/data:/app/overlay/data:ro"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    networks:
      - stock-ml-network
    restart: unless-stopped

  dashboard:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.dashboard
    container_name: stock-ml-dashboard
    ports:
      - "9001:3000"
    volumes:
      - ./stock_ml/dashboard:/app
      - ./results:/app/results:ro
    depends_on:
      - api
    networks:
      - stock-ml-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/leaderboard.html"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  nginx:
    image: nginx:alpine
    container_name: stock-ml-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - dashboard
    networks:
      - stock-ml-network
    restart: unless-stopped

networks:
  stock-ml-network:
    driver: bridge

volumes:
  postgres_data:
  data:
  results:
  logs:
