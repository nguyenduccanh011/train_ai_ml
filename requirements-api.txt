# API Server Dependencies
# FastAPI stack for production REST API

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# Security & Rate Limiting
slowapi==0.1.9

# Database
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.19.0  # Async SQLite driver (recommended for local research)
asyncpg==0.29.0    # PostgreSQL async driver (optional, for production)
psycopg2-binary==2.9.9  # PostgreSQL sync driver (optional)
alembic==1.13.1

# Async tasks (future)
# celery==5.3.4
# redis==5.0.1

# Data/Math (required by market_profile and config_loader)
numpy==1.26.3
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2
pyyaml==6.0.1
pyarrow==14.0.1  # Required for parquet file I/O in backtest pipelines
duckdb==1.5.3    # OHLCV OLAP store (read-only) — replaces per-symbol CSV reads

# Monitoring
psutil==5.9.6

# API documentation (auto-generated)
# Already included in FastAPI

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
