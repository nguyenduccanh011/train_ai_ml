## 2026-04-28 — Phase 6 Dashboard + CI/CD + Cleanup

### Done

**Phase 6.1 — Dashboard compatibility**:
- `python -m stock_ml export [--versions v22,v34] [--include-retired]` — subcommand mới gọi unified_export.
- `python -m stock_ml run champions/v22 --save-results --export` — pipeline → save CSV → generate viz JSON.
- `_export_to_dashboard()` helper: dùng `get_model_config()` + `export_version()` + `generate_manifest()` từ unified_export.

**Phase 6.2 — CI/CD**:
- `.github/workflows/ci.yml`: 4 jobs — lint (ruff), typecheck (mypy), test-unit (no integration), test-regression (hash check, push-only).
- Trigger: push master/refactor/**, PR → master.
- Cache golden với `actions/cache@v4`.

**Phase 6.3 — Cleanup (partial)**:
- Xóa 5 archive/ subfolder: analysis, exports, misc, src, visualization → ~128MB freed.
- archive/ còn: results_legacy/ (historical) + scripts/ (model configs).
- Regression 12/12 pass sau cleanup.

### Decision

- Không tag v2.0 / merge main vì chưa có explicit instruction.
- Không xóa experiments/ hay tạo legacy/ structure vì user chỉ chọn archive cleanup.

### Next

- Tag `v2.0` khi user ready.
- Merge `refactor/phase-0.2-golden` → master.
