# 2026-04-28 — Phase 0.3 Tooling setup

## Done

- Branch: tiếp tục `refactor/phase-0.2-golden` (chưa tạo phase-0.3 branch riêng theo user
  request — sub-branch overhead không cần thiết khi tooling setup chỉ 1 ngày).
- **`pyproject.toml`** (root train_ai_ml/): ruff lint (E/F/I/N/W/UP/B/SIM, line=100, py311),
  ruff format, mypy (strict cho `stock_ml/src/components` + `stock_ml/src/pipeline` — Phase 1+),
  pytest (testpaths=stock_ml/tests, markers `regression`/`quick`).
- **`stock_ml/requirements-dev.txt`**: pin 6 deps tested 2026-04-28 — ruff 0.15.12, mypy 1.20.2,
  pytest 7.4.3, pytest-xdist 3.8.0, pre-commit 4.6.0, pydantic 2.12.5. Pin manually thay vì
  `pip freeze` để tránh kéo cả env.
- **`.pre-commit-config.yaml`** (root): 3 hooks
  - `ruff` (--fix): lint + auto-fix safe issues
  - `ruff-format`: consistent formatting
  - `regression-champions` (local): hash check 11 trades CSV vs golden, chỉ trigger khi touch
    `stock_ml/(src|run_pipeline.py|config)` hoặc test_champions.py. <1s không full pipeline (~6m).
- **`stock_ml/tests/regression/test_champions.py`**: parametrize 11 versions, sha256 hash CSV ở
  `results/` vs golden checksums. Skip nếu CSV missing (kèm hint regen command). Sanity test
  `test_golden_dir_has_all_champions` verify checksums.txt đủ 11 entries.
- **Ruff auto-fixes 479 issues trên 83 files** (combine-if, sort-imports, syntax-upgrade,
  unused-vars khử). Production code logic NOT changed → verify bằng regen 11 champions CPU mode
  (336.3s) → `pytest test_champions.py` 12/12 pass, hash match golden 100%.
- **Ruff format 90 files** thống nhất style (auto-applied).
- **Per-file-ignores** cho legacy code (`SIM`, `B007`, `F841`, `N816`, `E741`, `E702`, `W293`,
  `N814`, `B905`): legacy.py, backtest/engine.py, features/engine.py, experiments/, analysis/,
  run_pipeline.py, model_manager.py. Sẽ siết khi port qua `src/components/` ở Phase 1+.
- **Pre-commit installed**: `git config --unset-all core.hooksPath` (đang trỏ default path rồi
  unset, không thay đổi behavior) → `pre-commit install` → hook ở `.git/hooks/pre-commit`.
- **`pre-commit run --all-files`**: 3/3 hooks Passed.

## Stuck

- **Windows file lock** với ruff format (3-5 lần). Pattern: `os error 1224 user-mapped section
  open`. Workaround: sleep 2-5s rồi retry. Nguyên nhân giả thuyết là antivirus/Windows Search
  hold file briefly. Không phải blocker, chỉ làm chậm 1 phút.
- `pre-commit install` lần đầu fail vì `core.hooksPath` đã set tới default `.git/hooks` →
  pre-commit từ chối (cowardly refusing). Unset → install OK.

## Decision

- **Per-file-ignores rộng cho legacy code thay vì full strict**: 244/424 ruff errors ở
  `src/strategies/legacy.py` toàn `SIM102` (collapsible-if). Strict toàn bộ = 1 ngày auto-fix +
  3 ngày verify behavior + regen golden. Trade-off: legacy giữ nguyên (sẽ migrate qua components
  Phase 1+), code mới strict full ruleset. Đúng spirit roadmap "không backwards compat
  burden".
- **Regression-champions hook chỉ hash check, không rerun pipeline**: rerun mỗi commit (~6m) =
  block dev flow. Hash check ~1s, pre-commit nhanh. User regen pipeline thủ công sau khi sửa
  src/. Trade-off: nếu code thay đổi behavior nhưng không regen → hook không catch. Acceptable
  vì refactor target là "không thay đổi behavior", regen là phần ý thức của workflow.
- **Branch không tách phase-0.3**: tooling setup overhead < 1 ngày, sub-branch không value-add.
  Tiếp tục refactor/phase-0.2-golden cho Phase 0.3-0.5.
- **Mypy chưa enable strict cho src/ legacy**: `files = ["stock_ml/src/components", "stock_ml/
  src/pipeline"]` — chưa exist ở Phase 0. Nghĩa là mypy chạy 0 errors thoải mái cho tới Phase 1.

## Verification log

```
$ pre-commit run --all-files
ruff (legacy alias)..............................................................Passed
ruff format......................................................................Passed
regression-champions (hash check 11 trades CSVs vs golden).......................Passed

$ python -m pytest stock_ml/tests/regression/test_champions.py -v
12 passed in ~1s

$ python -m ruff check stock_ml/
All checks passed!

$ python -m ruff format --check stock_ml/
90 files already formatted
```

## Risk note for downstream phases

- Khi Phase 1.2 viết feature blocks vào `src/components/features/`, mypy sẽ active strict —
  type hint phải đầy đủ từ đầu, không lazy.
- Khi Phase 2.x port fusion strategies, nhớ regen golden + verify hash sau mỗi merge — pre-commit
  hook chỉ catch hash mismatch nếu đã regen. Nếu refactor đổi format CSV (vd: thêm column) →
  regen golden + commit checksums.txt mới.
- Per-file-ignores cho `experiments/` rộng tới mức ignore cả `F` (logic bugs như undefined
  name). Nếu sau này cleanup `experiments/` (CLEANUP_PLAN.md step 6), có thể siết lại.

## Phase 0.5 piggy-backed (cùng ngày)

User đã đồng thời lock ARCHITECTURE.md section 15 → Architecture decisions locked:

1. **Symbol profile dispatch**: outer layer (orchestrator merge `profile_overrides` vào fusion
   params trước khi chạy). Giữ fusion thuần theo `BarContext`.
2. **Per-symbol model**: support by design (base contract chấp nhận wrapper
   `dict[symbol, model]`), defer build ở Phase 1.
3. **Ensemble entry**: native skeleton từ đầu (`EnsembleEntryModel` voting cơ bản trong
   `src/components/models/`).
4. **Dashboard manifest**: giữ format cũ trong transition (ít nhất tới hết Phase 6.1) để giảm
   risk gãy workflow quan sát.
5. **Real-time inference**: tách training mode vs inference mode; inference reuse
   fusion/backtest contracts với single-bar context.

`CHAMPION_VERSIONS.md` cũng sync với `EXIT_MODEL_BUG.md` — v37a_exit/v42_a hiện là
"trained-but-dropped" thay vì "active Model B".

## Next (cũ 2026-04-28)

- Phase 0.4 — Branch + tag: tag `arch-locked-baseline` sau khi commit golden + tooling +
  arch-lock (skip tạo branch riêng cho 0.3-0.5, đã thảo luận).
- Phase 1.1 — Base interfaces: `src/components/base.py` với BarContext / FusionResult /
  Position / Trade / Action dataclasses. Mục tiêu mypy strict pass cho `src/components/`.
- Phase 1.2 — Feature blocks: audit columns mỗi `leading*` set rồi tách thành blocks
  (OhlcvBasic, Momentum, Volume, Regime, HeikinAshi). Equivalence test với engine cũ.

---

# Phase 3 — Pipeline Orchestrator + CLI (2026-04-28 update)

## Done

### Phase 3.1 Orchestrator core
- [src/pipeline/config.py](../../src/pipeline/config.py): `ExperimentConfig` Pydantic load từ YAML, auto-suy `runner`.
- [src/pipeline/walker.py](../../src/pipeline/walker.py): `walk_forward()` wrapper.
- [src/pipeline/trainer.py](../../src/pipeline/trainer.py): `build_prediction_cache()` — refactor legacy `_build_predictions`.
- [src/pipeline/orchestrator.py](../../src/pipeline/orchestrator.py): `Pipeline` + `PipelineResult`, `CHAMPION_RUNNER_MAP` 11 champions.
- [tests/regression/test_pipeline_v22_parity.py](../../tests/regression/test_pipeline_v22_parity.py): 1 passed, 1784 trades.

### Phase 3.2 Caching layer
- [src/pipeline/cache.py](../../src/pipeline/cache.py): `PredictionCacheManager` pickle + atomic write.

### Phase 3.3 Matrix expander
- [src/pipeline/matrix_expander.py](../../src/pipeline/matrix_expander.py): `expand_matrix()` → `list[ExperimentConfig]`.
- [config/experiments/matrix/test_2x2.yaml](../../config/experiments/matrix/test_2x2.yaml): 2×2=4 configs OK.

### Phase 3.4 CLI
- [scripts/cli.py](../../scripts/cli.py): `run`, `run-matrix`, `validate`, `list-components`, `list-experiments`, `compare`.
- [__main__.py](../../__main__.py): `python -m stock_ml <cmd>`.

### Phase 3.5 Validation rules
- [src/pipeline/validate.py](../../src/pipeline/validate.py): `validate_config()` bắt 4 loại lỗi tại load time.

## Decision

- **Trainer reuse legacy `_build_predictions` qua wrapper**: không duplicate logic, dễ maintain.
- **PredictionCacheManager dùng pickle**: nhanh, đủ cho research phase.
- **`runner` field default ""**: matrix expander không cần khai báo `runner`, tự suy từ `strategy`.
- **CLI dùng `argparse`**: không thêm dependency mới.
- **Validation rule exit_model relaxed**: theo EXIT_MODEL_BUG.md, behavior hiện tại là trained-but-dropped — giữ nguyên cho parity.

## Next

- Phase 4.1 — Legacy adapter cho 49 version cũ.
- Phase 4.2 — Migration tool: flag → ExperimentConfig mapping.

---

# Phase 4 — Legacy adapter + migration tool + deprecate run_pipeline.py

## Done

### Phase 4.1 — LegacyVersionAdapter
- [src/pipeline/legacy_adapter.py](../../src/pipeline/legacy_adapter.py): `LegacyVersionAdapter` wrap tất cả 60+ legacy `backtest_vXX` functions qua Pipeline interface. Đọc config từ models.yaml, build prediction cache qua `trainer.build_prediction_cache`, dispatch backtest với mod_kwargs + proba_thresholds. `build_experiment_config()` để dùng với migrate_legacy. Warn khi dùng cho champion version.
- [src/pipeline/__init__.py](../../src/pipeline/__init__.py): export `LegacyVersionAdapter`, `LegacyRunResult`.
- [tests/components/test_legacy_adapter.py](../../tests/components/test_legacy_adapter.py): 18/18 pass.

### Phase 4.2 — Migration tool
- [scripts/migrate_legacy.py](../../scripts/migrate_legacy.py): `migrate_version()` + `migrate_all()` — convert models.yaml entry → ExperimentConfig YAML. `--dry-run` support. 49 non-champion versions migrate OK.
- [scripts/cli.py](../../scripts/cli.py): thêm `list-legacy`, `migrate-legacy`, `run legacy/vXX` route.

### Phase 4.3 — Deprecate run_pipeline.py
- [run_pipeline.py](../../run_pipeline.py): `DeprecationWarning` khi import, docstring redirect sang `python -m stock_ml`. File vẫn functional — xóa ở Phase 6.3.

## Verification
- `python -m stock_ml list-legacy` → 49 non-champion keys.
- `python -m stock_ml migrate-legacy v25 --dry-run` → YAML đúng schema.
- `python -m stock_ml migrate-legacy --all --dry-run` → 49 migrated, 11 champions skipped.
- `pytest tests/components/test_legacy_adapter.py -v` → 18 passed.
- `ruff check` + `mypy src/pipeline/legacy_adapter.py` → clean.

## Stuck
- Không.

## Decision
- **Adapter không phải exact-parity runner**: explicit doc "không exact-parity, dùng cho historical comparison". Champions có dedicated parity runner; adapter chỉ cho legacy non-champion.
- **`_meta` block trong migrate YAML**: giữ label/description/active/retired_reason từ models.yaml để không mất context — ExperimentConfig bỏ qua field unknown, không crash.
- **`rule` không có trong LEGACY_STRATEGY_MAP**: `rule` champion có entry point khác (`compare_rule_vs_model`), không phải `backtest_vXX` pattern. Adapter không cover `rule` — đúng behavior.
- **DeprecationWarning ở module level**: warning phát ra khi `import run_pipeline` (kể cả indirect). Acceptable vì target là rõ ràng redirect developers sang CLI mới.

## Files touched
- `stock_ml/src/pipeline/legacy_adapter.py` (new)
- `stock_ml/src/pipeline/__init__.py` (edit)
- `stock_ml/scripts/migrate_legacy.py` (new)
- `stock_ml/scripts/cli.py` (edit — thêm legacy route + list-legacy + migrate-legacy)
- `stock_ml/tests/components/test_legacy_adapter.py` (new)
- `stock_ml/run_pipeline.py` (edit — deprecation warning + docstring)
- `stock_ml/docs/refactor/README.md` (edit — status update)
- `stock_ml/docs/refactor/REFACTOR_ROADMAP.md` (edit — Phase 4 DONE + result blocks)
- `stock_ml/docs/refactor/diary/2026-04-28.md` (edit — this entry)

## Next

- Phase 5 — Testing & verification: full regression test toàn bộ 11 champions, performance benchmark, documentation.
- Phase 5.1: `pytest tests/regression/` toàn bộ champions + pipeline.
- Phase 5.2: benchmark timing v22 cũ vs mới.
- Phase 5.3: HOW_TO_* guides nếu cần.
