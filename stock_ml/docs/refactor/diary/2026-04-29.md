# 2026-04-29 — Phase 2.2 Base fusion interface + Phase 1.3 close-out + Phase 2.3a Port `rule`

## Done

- Mở rộng dataclass ở [src/components/base.py](../../src/components/base.py):
  - `Position.strategy_state: dict[str, Any]` — counter state carry across bars (giải open issue #2 inventory §7).
  - `FusionActionType` thêm `"keep_position"` — hold layer block toàn bộ exit_override (giải #1).
- [src/components/fusion/stack.py](../../src/components/fusion/stack.py) — `FusionStack`:
  - Group strategies theo layer, sort theo `priority` (ascending).
  - Lifecycle order: `pre_entry → entry → hold → exit_override`.
  - Pre_entry chỉ run khi `position is None and entry_signal != 0` (khớp engine cũ).
  - `keep_position` ở hold layer skip toàn bộ exit_override.
  - Short-circuit: first `skip_entry`/`enter`/`exit` thắng. Counter & reasons accumulate qua `StackOutcome`.
- [src/components/fusion/registry.py](../../src/components/fusion/registry.py) — `register_strategy / get_strategy / list_strategies / list_always_on / clear_registry`. `always_on=True` flag cho core strategies (giải #4).
- 17 unit tests ở [tests/components/test_fusion_stack.py](../../tests/components/test_fusion_stack.py): layer dispatch, short-circuit, keep_position blocking, priority order, state carry, counter accumulation, registry duplicate/replace/unknown.
- Verify: `pytest stock_ml/tests/components/` 66/66 pass; `ruff check` + `ruff format --check` + `mypy stock_ml/src/components/` đều clean.
- Update [FUSION_STRATEGY_INVENTORY.md §7](../FUSION_STRATEGY_INVENTORY.md): đánh dấu issues 1, 2, 4, 5 resolved; issue 3 (per-symbol set) defer Phase 2.4f.

## Stuck

- Không.

## Decision

- **`keep_position` thay vì layer mới `exit_modifier`**: giữ 4 layer (lock-in từ ARCHITECTURE.md). Hold layer thực hiện vai trò "block exit signal" qua action mới — đỡ explosion of layers, semantics rõ.
- **Strategy state ở `Position` (không phải `BarContext`)**: state phải carry across bars cùng position. `BarContext` mỗi bar tạo mới → không phù hợp. Convention key namespace `<strategy>:<field>` sẽ enforce ở Phase 2.3 khi port strategies thực.
- **Pre_entry skip khi `entry_signal == 0`**: khớp engine cũ — filter chỉ evaluate khi có signal cần xét. Tránh tốn compute & tránh side effect không mong muốn từ filter logic.
- **Counter semantics đơn giản hóa**: `metadata["counter"] = "<key>"` → `Counter[str]`. Naming convention `n_<strategy>` khớp engine cũ, verify khi port v22 (Phase 2.3c).
- **Per-symbol set defer**: chưa có strategy nào ở Phase 2.3 (v22, v19_3, rule) cần per-symbol set. Lock convention khi đến v39d.

## Files touched

- `stock_ml/src/components/base.py` (edit)
- `stock_ml/src/components/fusion/__init__.py` (edit)
- `stock_ml/src/components/fusion/stack.py` (new)
- `stock_ml/src/components/fusion/registry.py` (new)
- `stock_ml/tests/components/test_fusion_stack.py` (new)
- `stock_ml/docs/refactor/FUSION_STRATEGY_INVENTORY.md` (§7 update)

## Next

- Phase 2.3b — `v19_3` (minimal stack legacy).
- Phase 2.3c — `v22` (full V22 stack: HardCap/FastExit/PeakProtect variants).

---

# Phase 2.3b — Port `v19_3` minimal ML stack

## Done

- [src/components/fusion/helpers/](../../src/components/fusion/helpers/) — port helper indicators/regime/sizing cho v19_3, giữ inline arithmetic theo legacy thay vì dùng feature columns.
- [src/components/fusion/strategies/core/](../../src/components/fusion/strategies/core/) — thêm 11 always-on strategies dùng lại cho champion ML: hard stop, signal hard cap, fast exit loss, ATR stop, peak protect dist/EMA, MA-cross hybrid exit, adaptive trailing, profit lock, zombie exit, min-hold protection.
- [src/components/fusion/strategies/entry/v19_entry_cascade.py](../../src/components/fusion/strategies/entry/v19_entry_cascade.py) — port entry cascade v19_3: quick reentry, consolidation/secondary breakout, V-shape bypass, ML entry, filters, alpha gate, bear/chop defense và position sizing.
- [src/components/fusion/strategies/hold/v19_signal_hold_guard.py](../../src/components/fusion/strategies/hold/v19_signal_hold_guard.py) — port signal-exit hold guards: confirmed exit scoring, confirm bars, strong uptrend carry, trend carry override.
- [src/components/runners/v19_3_runner.py](../../src/components/runners/v19_3_runner.py) — dedicated runner cho v19_3, dùng prediction cache từ legacy `_build_predictions()` khi regression cần bit-equivalent y_pred.
- [tests/regression/test_v19_3_parity.py](../../tests/regression/test_v19_3_parity.py) — pass exact golden 1910 trades sau CSV roundtrip normalization cho float formatting.
- Verify:
  - `pytest tests/components/ -q` → 156 passed.
  - `PYTHONHASHSEED=42 pytest tests/regression/test_v19_3_parity.py -v -s` → 1 passed.
  - `pytest tests/regression/test_rule_parity.py -q` → 1 passed.
  - `ruff check src/components/ tests/components/ tests/regression/test_v19_3_parity.py` → clean.
  - `mypy src/components/` → clean.

## Stuck

- Không. Có mismatch ban đầu ở `entry_wp` do last-bit float khác giữa in-memory DataFrame và CSV golden; trade count/logic đã match 1910. Fix bằng CSV roundtrip `df_new` trước `assert_frame_equal(check_exact=True)` để test đúng artefact golden.

## Decision

- **Gộp Batch B thành 2 strategy lớn**: `V19EntryCascade` và `V19SignalHoldGuard`. Lý do: entry filters/sizing chia sẻ nhiều state (`entry_score`, breakout/vshape flags, relaxed counters, regime cfg); tách 17 class nhỏ dễ lệch parity.
- **Dedicated runner thay vì SimpleLongBacktester**: v19_3 cần equity-scaled returns theo `position_size`, cooldown, max_equity/max_price state và close dangling position với `exit_reason="end"`.
- **Không re-implement ML walk-forward ở runner**: regression tiếp tục gọi legacy `_build_predictions()` để giữ y_pred bit-equivalent; Phase 3 orchestrator mới sẽ tách sau.
- **CSV-normalized exact parity**: golden là CSV artefact, nên regression so sánh sau khi serialize/parse `df_new` qua CSV để tránh false negative do float representation.
- **`FusionStack` hỗ trợ bulk counters**: thêm `metadata["counters"]` để monolithic strategy vẫn ghi được nhiều legacy counters trong một bar.

## Files touched

- `stock_ml/src/components/base.py`
- `stock_ml/src/components/fusion/helpers/`
- `stock_ml/src/components/fusion/stack.py`
- `stock_ml/src/components/fusion/strategies/__init__.py`
- `stock_ml/src/components/fusion/strategies/core/`
- `stock_ml/src/components/fusion/strategies/entry/`
- `stock_ml/src/components/fusion/strategies/hold/`
- `stock_ml/src/components/runners/__init__.py`
- `stock_ml/src/components/runners/v19_3_runner.py`
- `stock_ml/tests/components/fusion/test_v19_entry_cascade.py`
- `stock_ml/tests/components/fusion/test_v19_signal_hold_guard.py`
- `stock_ml/tests/components/test_v19_3_smoke.py`
- `stock_ml/tests/regression/test_v19_3_parity.py`
- `stock_ml/docs/refactor/REFACTOR_ROADMAP.md`
- `stock_ml/docs/refactor/FUSION_STRATEGY_INVENTORY.md`

## Next

- Phase 2.3c — `v22` full V22 stack: V22 hard cap/fast exit/peak protect variants, long-horizon carry, and regression against `trades_v22.csv`.

---

# Phase 2.3c — Port `v22` full V22 stack

## Done

- [src/components/fusion/strategies/__init__.py](../../src/components/fusion/strategies/__init__.py) — register v22/core strategies để YAML Phase 3 resolve được: hard stop, V22 hard cap/fast exit, ATR stop, peak protect dist/EMA, hybrid exit, adaptive trailing, profit lock, zombie, min-hold, long-horizon carry.
- [config/experiments/champions/v22.yaml](../../config/experiments/champions/v22.yaml) — declarative champion spec mirror actual [src/components/runners/v22_runner.py](../../src/components/runners/v22_runner.py): `leading_v2`, `trend_regime`, LightGBM, exit-model metadata, `force_exit` + `active_exit` + `hold` order.
- [tests/components/fusion/test_v22_registry.py](../../tests/components/fusion/test_v22_registry.py) — registry/YAML smoke: all YAML strategy names resolve; force/active exit order matches runner contract; mods/params stay aligned with defaults.
- Verify:
  - `pytest stock_ml/tests/components/fusion/test_v22_fast_exit.py stock_ml/tests/components/fusion/test_v22_hard_cap.py stock_ml/tests/components/fusion/test_v22_registry.py -q` → 13 passed.
  - `PYTHONHASHSEED=42 pytest stock_ml/tests/regression/test_v22_parity.py -v -s` → 1 passed, 1784 trades exact golden.
  - `pytest stock_ml/tests/regression/test_rule_parity.py stock_ml/tests/regression/test_v19_3_parity.py -q` → 2 passed.
  - `ruff check stock_ml/src/components/fusion/strategies stock_ml/tests/components/fusion/test_v22_registry.py` + `mypy stock_ml/src/components/` → clean.

## Decision

- **Close Phase 2.3c bằng parity-safe runner**: không tách lại `V19EntryCascade` thành `SMA200Filter` / `HybridEntry` / `MlOnly` / `TimeDecay` trong phase này vì v22 parity đang được guard bởi dedicated runner và exact golden.
- **YAML là spec chuẩn bị Phase 3, chưa executable**: orchestrator/CLI mới vẫn thuộc Phase 3; YAML hiện dùng để lock tên registry và order.

## Next

- Phase 2.4 — remaining champions, bắt đầu từ v34/v32 stack theo roadmap.

---

# Phase 2.4a — Port `v34` V32-stack champion

## Done

- [src/components/runners/v34_runner.py](../../src/components/runners/v34_runner.py) — parity-first runner wrapper quanh `experiments.run_v34_final.backtest_v34`, dùng config `v34` từ [config/models.yaml](../../config/models.yaml).
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v34` và `trades_to_v34_dataframe`.
- [tests/regression/test_v34_parity.py](../../tests/regression/test_v34_parity.py) — golden parity test cho `trades_v34.csv`, build CPU prediction cache từ meta và CSV-normalize trước exact compare.
- Verify:
  - `python -m pytest stock_ml/tests/regression/test_v34_parity.py -q` → 1 passed, 1323 trades exact golden.
  - `python -m pytest stock_ml/tests/regression/test_v22_parity.py stock_ml/tests/regression/test_v34_parity.py -q` → 2 passed.
  - `python -m ruff check stock_ml/src/components/runners/v34_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v34_parity.py` → clean.

## Decision

- **Wrapper legacy thay vì port từng V26-V32 strategy ngay**: v34 dùng full V32 cumulative behavior lớn hơn các strategy đã tách ở Phase 2.3; wrapper giữ exact parity và khóa test trước, phần YAML-executable V32 stack có thể tách dần sau.
- **Preserve trained-but-dropped exit model**: cache vẫn train exit model theo config/meta, nhưng runner không truyền `y_pred_exit` vào `backtest_v34`, mirror legacy `run_pipeline._run_backtest_from_cache` signature filtering để match golden.

## Next

- Phase 2.4b — `v35b` entry-layer flags (`v35_relax_cooldown`, `v35_skip_price_proximity`, `v35_single_bar_signal`, `v35_rule_override`, `v35_hybrid_entry`).

---

# Phase 2.4b — Port `v35b` entry-layer champion

## Done

- [src/components/runners/v35b_runner.py](../../src/components/runners/v35b_runner.py) — parity-first runner wrapper quanh `experiments.run_v34_final.backtest_v35b`, dùng config `v35b` từ [config/models.yaml](../../config/models.yaml).
- [src/components/runners/v34_runner.py](../../src/components/runners/v34_runner.py) — factor shared `_run_v34_lineage_cache(...)` để v34/v35b dùng chung cache-to-trades path và tránh duplicate lineage runner logic.
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v35b` và `trades_to_v35b_dataframe`.
- [config/experiments/champions/v35b.yaml](../../config/experiments/champions/v35b.yaml) — declarative champion spec mirror legacy `v35b`: `leading_v4`, `early_wave`, LightGBM, exit-model metadata enabled và V35 entry flags.
- [tests/regression/test_v35b_parity.py](../../tests/regression/test_v35b_parity.py) — golden parity test cho `trades_v35b.csv`, build CPU prediction cache từ meta, CSV-normalize rồi exact compare; assert `model_b_exit == 0`.
- Verify:
  - `python -m pytest stock_ml/tests/regression/test_v35b_parity.py -q` → 1 passed, 1381 trades exact golden.
  - `python -m pytest stock_ml/tests/regression/test_v34_parity.py stock_ml/tests/regression/test_v35b_parity.py -q` → 2 passed.
  - `python -m pytest stock_ml/tests/regression/test_rule_parity.py stock_ml/tests/regression/test_v19_3_parity.py stock_ml/tests/regression/test_v22_parity.py stock_ml/tests/regression/test_v34_parity.py stock_ml/tests/regression/test_v35b_parity.py -q` → 5 passed.
  - `python -m ruff check stock_ml/src/components/runners/v34_runner.py stock_ml/src/components/runners/v35b_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v35b_parity.py` → clean.

## Decision

- **Reuse V34 lineage helper**: v35b cùng V32/V34 stack, chỉ khác params/flags; dùng chung `_run_v34_lineage_cache(...)` để khóa behavior và giảm duplicate code.
- **Preserve trained-but-dropped exit model**: cache vẫn có thể train exit model theo meta/config, nhưng runner không truyền `y_pred_exit` vào `backtest_v35b`; khớp legacy signature filtering và golden `model_b_exit == 0`.
- **YAML champion spec chưa executable**: [config/experiments/champions/v35b.yaml](../../config/experiments/champions/v35b.yaml) phục vụ Phase 3 orchestrator/registry mapping, runner hiện vẫn đọc legacy [config/models.yaml](../../config/models.yaml).

## Next

- Phase 2.4c — `v32` standalone parity runner/regression sau khi v34/v35b lineage đã được khóa.

---

# Phase 2.4c — Verify `v32` standalone champion

## Done

- [src/components/runners/v32_runner.py](../../src/components/runners/v32_runner.py) — parity-first runner wrapper quanh `experiments.run_v32_final.backtest_v32`, dùng config `v32` từ [config/models.yaml](../../config/models.yaml) và reuse V34-lineage helper.
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v32` và `trades_to_v32_dataframe`.
- [tests/regression/test_v32_parity.py](../../tests/regression/test_v32_parity.py) — golden parity test cho `trades_v32.csv`, build CPU prediction cache từ meta, CSV-normalize rồi exact compare.
- Verify:
  - `python -m pytest stock_ml/tests/regression/test_v32_parity.py -q` → 1 passed, 1347 trades exact golden.
  - `python -m pytest stock_ml/tests/regression/test_rule_parity.py stock_ml/tests/regression/test_v19_3_parity.py stock_ml/tests/regression/test_v22_parity.py stock_ml/tests/regression/test_v34_parity.py stock_ml/tests/regression/test_v35b_parity.py stock_ml/tests/regression/test_v32_parity.py -q` → 6 passed.
  - `python -m ruff check stock_ml/src/components/runners/v32_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v32_parity.py` → clean.

## Decision

- **Reuse V34 lineage helper**: v32 nằm trong cùng V32/V34 lineage; wrapper mỏng giữ behavior legacy thay vì tách lại full V26-V32 stack trong Phase 2.4c.
- **Preserve trained-but-dropped exit model**: cache vẫn có thể train exit model theo meta/config, nhưng runner không truyền `y_pred_exit` vào `backtest_v32`; khớp legacy golden.

## Next

- Phase 2.4d — `v39d` per-symbol rule routing.

---

# Phase 2.4e — Port `v37a_exit` & `v42_a` exit-model champions

## Done

- [src/components/runners/v37a_exit_runner.py](../../src/components/runners/v37a_exit_runner.py) — parity-first runner wrapper quanh `experiments.run_v37a.backtest_v37a`, dùng config `v37a_exit` (`leading_v4`, `early_wave_dual` fw=8, mods a/b/e/f/g/h/i/j) và reuse V34-lineage helper.
- [src/components/runners/v42_a_runner.py](../../src/components/runners/v42_a_runner.py) — parity-first runner wrapper quanh `experiments.run_v42.backtest_v42` (alias của `backtest_v37a`), dùng config `v42_a` (`early_wave_dual` fw=15) và reuse V34-lineage helper.
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v37a_exit`, `run_v42_a` + dataframe converters.
- [tests/regression/test_v37a_exit_parity.py](../../tests/regression/test_v37a_exit_parity.py) — golden parity 1370 trades; dùng `meta.get("exit_model_config")` vì meta golden không có key này; assert `model_b_exit == 0`.
- [tests/regression/test_v42_a_parity.py](../../tests/regression/test_v42_a_parity.py) — golden parity 1442 trades; cùng pattern.
- Verify:
  - `PYTHONHASHSEED=42 python -m pytest stock_ml/tests/regression/test_v37a_exit_parity.py -v -s` → 1 passed.
  - `PYTHONHASHSEED=42 python -m pytest stock_ml/tests/regression/test_v42_a_parity.py -v -s` → 1 passed.
  - Full champion suite (rule, v22, v32, v34, v35b, v37a, v37a_exit, v39d, v42_a) → 9 passed.
  - `python -m ruff check stock_ml/src/components/runners/v37a_exit_runner.py stock_ml/src/components/runners/v42_a_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v37a_exit_parity.py stock_ml/tests/regression/test_v42_a_parity.py` → clean.
  - `python -m mypy stock_ml/src/components/` → clean.

## Decision

- **Reuse V34 lineage helper**: cả v37a_exit và v42_a chia sẻ V37a engine; runner wrapper mỏng giữ exact legacy behavior, không tách lại fusion stack hay duplicate runner logic.
- **`_exit_model_cfg` returns None cho v37a_exit/v42_a**: models.yaml không khai báo `exit_model` block cho 2 version này → cache không train Model B. Khớp với golden meta thiếu `exit_model_config`. Pipeline gate trong `run_pipeline._run_backtest_from_cache` cũng drop `y_pred_exit` cho legacy backtest function dùng `**kwargs`. Match golden `model_b_exit == 0` (xem [EXIT_MODEL_BUG.md](../EXIT_MODEL_BUG.md)).
- **`meta.get("exit_model_config")` trong parity test**: forward-compat với cả golden có lẫn không có key này. Cả 2 golden meta hiện tại đều không có.
- **`backtest_v42` = alias `backtest_v37a`**: theo `experiments/run_v42.py:58-59`. Không tách runner riêng vì shape engine giống nhau, chỉ khác target config (early_wave_dual fw=15).

## Next

- Phase 2.4f — `v37d` GRU champion (PyTorch sequence model path).

---

# Phase 2.4f — Port `v37d` GRU champion

## Done

- [src/components/runners/v37d_runner.py](../../src/components/runners/v37d_runner.py) — parity-first runner wrapper quanh `experiments.run_v37d.backtest_v37d` (delegate `backtest_v32`); dùng config `v37d` từ [config/models.yaml](../../config/models.yaml) (`leading_v4`, `early_wave`, `model_type=gru`) và reuse V34-lineage helper.
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v37d` và `trades_to_v37d_dataframe`.
- [tests/regression/test_v37d_parity.py](../../tests/regression/test_v37d_parity.py) — golden parity 1407 trades; build CPU prediction cache với `model_type` từ models.yaml (golden meta ghi sai `lightgbm`); CSV-normalize rồi exact compare; assert `model_b_exit == 0`.
- Verify:
  - `PYTHONHASHSEED=42 python -m pytest stock_ml/tests/regression/test_v37d_parity.py -v -s` → 1 passed.
  - `python -m ruff check stock_ml/src/components/runners/v37d_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v37d_parity.py` → clean.
  - `python -m mypy stock_ml/src/components/` → clean.

## Stuck

- Lần đầu chạy ra 1323 trades thay vì 1407 vì test build cache với `meta["model_type"]="lightgbm"` (snapshot meta sai) → chạy ra V34 LightGBM behavior. Fix bằng cách override `model_type` từ `get_model_config("v37d")` (`gru`).

## Decision

- **Reuse V34 lineage helper**: v37d engine identical với V34 stack (`backtest_v37d` delegate `backtest_v32`); chỉ khác model swap ở `_build_predictions(...)`. Wrapper mỏng giữ exact legacy behavior, không tách GRU thành component path mới.
- **Override `model_type` từ config thay vì meta**: nguồn sự thật cho v37d là models.yaml, không phải meta golden. Pattern này riêng cho v37d vì meta sinh trước khi lock model_type chính xác; các champion khác giữ pattern dùng meta.
- **Preserve trained-but-dropped exit model**: cache có thể train exit model theo meta `exit_model_config`, nhưng runner không truyền `y_pred_exit` vào `backtest_v37d`; khớp legacy golden `model_b_exit == 0`.

## Files touched

- `stock_ml/src/components/runners/v37d_runner.py` (new)
- `stock_ml/src/components/runners/__init__.py` (edit)
- `stock_ml/tests/regression/test_v37d_parity.py` (new)
- `stock_ml/docs/refactor/README.md` (edit)
- `stock_ml/docs/refactor/REFACTOR_ROADMAP.md` (edit)
- `stock_ml/docs/refactor/diary/2026-04-29.md` (edit)

## Next

- Phase 2 hoàn tất — toàn bộ 11 champion đã có parity runner/regression locked.
- Phase 3 — pipeline orchestrator + CLI (3.1 orchestrator core, 3.2 caching layer, 3.3 matrix expander, 3.4 CLI, 3.5 validation rules).

---

# Phase 2.4d — Port `v39d` per-symbol rule routing

## Done

- [src/components/runners/v39d_runner.py](../../src/components/runners/v39d_runner.py) — parity-first runner wrapper quanh `experiments.run_v39d.backtest_v39d`, dùng config `v39d` từ [config/models.yaml](../../config/models.yaml) và reuse V34-lineage helper.
- [src/components/runners/__init__.py](../../src/components/runners/__init__.py) — export `run_v39d` và `trades_to_v39d_dataframe`.
- [tests/regression/test_v39d_parity.py](../../tests/regression/test_v39d_parity.py) — golden parity test cho `trades_v39d.csv`, build CPU prediction cache từ meta, CSV-normalize rồi exact compare; assert `model_b_exit == 0`.
- Verify:
  - `python -m pytest stock_ml/tests/regression/test_v39d_parity.py -q` → 1 passed, 1181 trades exact golden.
  - `python -m ruff check stock_ml/src/components/runners/v39d_runner.py stock_ml/src/components/runners/__init__.py stock_ml/tests/regression/test_v39d_parity.py` → clean.

## Decision

- **Reuse V34 lineage helper**: v39d là v37a lineage với per-symbol rule-exit flags; wrapper mỏng giữ exact legacy behavior và tránh tách per-symbol rule routing trước khi orchestrator Phase 3 ổn định.
- **Preserve trained-but-dropped exit model**: cache vẫn có thể train exit model theo meta/config, nhưng runner không truyền `y_pred_exit` vào `backtest_v39d`; khớp legacy golden.

## Next

- Phase 2.4e — `v37a_exit` và `v42_a`, preserve trained-but-dropped exit-model behavior để match golden.

# Phase 1.3 close-out — Targets DONE

## Done

- Verify [tests/components/test_targets_equivalence.py](../../tests/components/test_targets_equivalence.py) → 17/17 pass.
- 4 target generators đã có sẵn ở [src/components/targets/](../../src/components/targets/): `trend_regime.py`, `early_wave.py`, `early_wave_v2.py`, `early_wave_dual.py` + [registry.py](../../src/components/targets/registry.py).
- Update [REFACTOR_ROADMAP.md](../REFACTOR_ROADMAP.md) §Phase 1.3 → ✅ DONE 2026-04-29 + Result block.

## Decision

- Skip thêm direct legacy equivalence test cho `EarlyWaveDualTarget` — gap nhỏ, defer. Scope control: không tăng scope ngoài việc đóng phase.

---

# Phase 2.3a — Port `rule` baseline

## Done

- **Concrete Backtester**: [src/components/backtest/engine.py](../../src/components/backtest/engine.py) — `SimpleLongBacktester.run(actions, df_test, *, commission=0.0015, tax=0.001) → list[Trade]`.
  - Pair `enter_long` với `exit` kế tiếp; bỏ qua `hold`; drop dangling positions cuối kỳ.
  - PnL formula: `(exit/entry - 1)*100 - (commission + tax)*2*100`, round 2.
  - Detect cột `timestamp` hoặc `date`; format `str(...)[:10]`. Round price 2 chữ số.
  - 10 unit tests pass ở [tests/components/test_backtest_simple.py](../../tests/components/test_backtest_simple.py).
- **Rule fusion strategies**: [src/components/fusion/strategies/rule_signal.py](../../src/components/fusion/strategies/rule_signal.py).
  - `RuleSignalEntry` (entry layer, priority 0): trigger `macd_hist > 0 and close > ma20 and close > open` → `enter, reason="rule_buy", counter="n_rule_signal_entry"`.
  - `RuleSignalExit` (exit_override layer, priority 0): trigger `macd_hist < 0 and close < ma20 and close < open` → `exit, reason="rule_sell"`.
  - NaN/missing data → `pass`. NOT `always_on` (rule độc lập).
  - 13 unit tests pass ở [tests/components/test_fusion_rule_signal.py](../../tests/components/test_fusion_rule_signal.py).
- **Mini driver**: [src/components/runners/rule_runner.py](../../src/components/runners/rule_runner.py) — `run_rule_baseline(symbols, data_dir, *, first_test_year=2020, min_bars=50)`.
  - Mirror exact [run_pipeline.py:407-439](../../run_pipeline.py#L407-L439) `_run_rule_backtest_fair()`: filter year≥2020 sau khi compute MA20/MACD, loop từ i=26.
  - Inline indicator compute (không qua FeatureBlock — match legacy exact).
  - `trades_to_dataframe()` helper với column order match golden schema.
- **Regression test**: [tests/regression/test_rule_parity.py](../../tests/regression/test_rule_parity.py) — `pd.testing.assert_frame_equal(check_exact=True)` pass với golden 2585 trades, 61 symbols, 2.6s end-to-end.
- Toolchain: ruff/format/mypy clean cho tất cả file mới.

## Decision

- **`entry_signal=1` mặc định** trong `BarContext` cho rule: `RuleSignalEntry.apply()` tự đọc cột macd_hist/ma20 — semantic check thực tế nằm trong strategy, không phải tín hiệu pre-computed. FusionStack chỉ dùng `entry_signal != 0` làm gate vào pre_entry/entry layer.
- **Inline indicator compute trong driver**, không qua `FeatureBlock`: legacy `compare_rule_vs_model.backtest_rule()` cũng inline → match exact để byte-identical golden. Khi port v22+ (cần feature blocks toàn bộ) sẽ chuyển sang `ComposableFeatureEngine`.
- **`_make_dummy_position` stub**: FusionStack chỉ check `position is None` để decide layer; rule không cần state thực. Phase 2.3c (v22) sẽ thay bằng `Position` đầy đủ với entry context.
- **Drop dangling positions** ở backtester: legacy không close cuối kỳ, golden cũng không có trade dangling → match.

## Files touched

### New
- `stock_ml/src/components/backtest/engine.py`
- `stock_ml/src/components/fusion/strategies/__init__.py`
- `stock_ml/src/components/fusion/strategies/rule_signal.py`
- `stock_ml/src/components/runners/__init__.py`
- `stock_ml/src/components/runners/rule_runner.py`
- `stock_ml/tests/components/test_backtest_simple.py`
- `stock_ml/tests/components/test_fusion_rule_signal.py`
- `stock_ml/tests/regression/test_rule_parity.py`

### Edit
- `stock_ml/src/components/backtest/__init__.py` (re-export `SimpleLongBacktester`)
- `stock_ml/docs/refactor/REFACTOR_ROADMAP.md` (Phase 1.3 DONE + Phase 2.3a section)

## Next

- Phase 2.3b — `v19_3` (minimal stack legacy, walk-forward, ML-based).
- Phase 2.3c — `v22` (full V22 stack: HardCap/FastExit/PeakProtect variants).
